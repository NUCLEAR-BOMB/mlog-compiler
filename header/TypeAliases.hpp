#pragma once

#include <string>
#include <sstream>

namespace mlc
{

using string_t = std::string;
using string_const_ref_t = const std::string_view;
using string_ref_t = std::string;

using char_t_ = char;

using string_stream_t = std::stringstream;

}
